{
  "name": "demo-ci-cd",
  "version": "1.0.0",
  "scripts": {
    "start": "node app/index.js",
    "build": "echo 'Building project...' && mkdir -p dist && cp index.js dist/",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "mocha": "^11.7.2"
  }
}
